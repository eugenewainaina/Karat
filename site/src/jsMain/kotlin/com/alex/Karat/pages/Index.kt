package com.alex.Karat.pages

import androidx.compose.runtime.*
import com.varabyte.kobweb.core.Page

@Page
@Composable
fun HomePage() {
    // TODO: add welcome screen

    /*var colorsss by remember { mutableStateOf(lss.random()) }

    // colour changing boxes
    Box(Modifier.fillMaxSize().backgroundColor(Colors.Black), contentAlignment = Alignment.Center) {
        Text("THIS PAGE INTENTIONALLY LEFT BLANK")

        Column {
            Row {
                Box(Modifier.backgroundColor(colorsss).size(100.px).onMouseOver { lss.random().also { colorsss = it } })
                Box(Modifier.backgroundColor(colorsss).size(100.px).onMouseOver { lss.random().also { colorsss = it } })
            }
            Row {
                Box(boxSize.toModifier().backgroundColor(Colors.Blue).onMouseOver {  })
                Box(boxSize.toModifier().backgroundColor(Colors.Yellow))
            }
        }
    }*/
}
